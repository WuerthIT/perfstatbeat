- key: system
  title: "system"
  description: >
    Performance metrics collected through the perfstat API of the AIX operating system
    and supplied in a form, that is mostly compatible with the metricbeat system module.
  fields:
    - name: system
      type: group
      description: >
        `system` contains local system metrics.
      fields:
